---
- hosts: localhost
  connection: local
  become: true

  pre_tasks:
  - name: Update Repositories
    tags: always
    apt:
      update_cache: yes
    changed_when: False

  tasks:
    - include: tasks/users.yml
    - include: tasks/cron.yml
